def preprocess_document(document):
    # Preprocess the document (e.g., tokenization)
    return processed_document

def preprocess_query(query):
    # Preprocess the user query
    return processed_query
